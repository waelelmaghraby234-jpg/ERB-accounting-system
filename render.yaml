services:
  - type: web
    name: holding-erp-api
    runtime: docker
    plan: starter
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: ADMIN_GROUP_CODE
        value: GROUP001
      - key: ADMIN_GROUP_NAME
        value: My Holding Group
      - key: AUTO_MIGRATE
        value: "true"
